defaults:
  - dataset: cifar10
  - model: glow
  - pert: clean
  - misc: gen_misc
  - hparams: ${defaults.0.dataset}_${defaults.1.model}
  - override hydra/job_logging: custom
  - override hydra/output: custom
ord: [inf, 1, 2]